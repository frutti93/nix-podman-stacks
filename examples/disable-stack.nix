/*
Each stack has a `disable` option that can be used to disable the stack.
E.g. in order to disable the Streaming stack, you can use the following configuration:
You can also remove the declaration entirely, since stacks are disabled by default
*/
{
  tarow.stacks.streaming.enable = false;
}
