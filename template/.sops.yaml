# Enter your age public key below.
# age key can be generated by using `age-keygen` or `ssh-to-age`.
# E.g. `age-keygen -o ~/.config/sops/age/keys.txt`

# This demo example includes the private age key in the `keys.txt` file. Make sure to generate your own keypair.
keys:
  - &myhost age1zp8hfpwdvtj48z9hjjgrcmsz8q2mpa22f38qtqjdsrme9adan4xszdv54g
creation_rules:
  - path_regex: secrets.yaml$
    key_groups:
      - age:
          - *myhost
